import 'package:flutter/material.dart';

class AdminUsersTemplates extends StatelessWidget {
  const AdminUsersTemplates({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Admin User Templates')),
      body: const Center(
        child: Text('Admin Control Panel — Coming Soon!'),
      ),
    );
  }
}
